<UserControl x:Class="CRDebugger.Wpf.Views.OptionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:CRDebugger.Core.ViewModels">

    <DockPanel Margin="20">
        <!-- ヘッダー -->
        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,16">
            <TextBlock Text="Options"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="{DynamicResource OnBackgroundBrush}"
                       VerticalAlignment="Center" />
            <Button Content="Refresh"
                    Style="{DynamicResource CRButton}"
                    Command="{Binding RefreshCommand}"
                    HorizontalAlignment="Right"
                    Padding="14,7" />
        </DockPanel>

        <!-- オプションカテゴリ一覧 -->
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding FilteredCategories}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:OptionCategoryViewModel}">
                        <Border Background="{DynamicResource SurfaceBrush}"
                                CornerRadius="8"
                                Margin="0,0,0,14"
                                Padding="18,14">
                            <StackPanel>
                                <!-- カテゴリヘッダー -->
                                <TextBlock Text="{Binding Name}"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource PrimaryBrush}"
                                           Margin="0,0,0,10" />

                                <!-- オプション項目（動的コントロール） -->
                                <ItemsControl x:Name="OptionItems"
                                              ItemsSource="{Binding FilteredItems}"
                                              Loaded="OnOptionItemsLoaded">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <ContentPresenter Content="{Binding}"
                                                              Loaded="OnOptionItemLoaded" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</UserControl>
